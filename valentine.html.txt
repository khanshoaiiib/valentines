<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Alfiya ❤️ Shoaib</title>

<style>
body {
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
    margin:0;
    font-family:sans-serif;
    background:linear-gradient(135deg,#ffe4ec,#fce4ec);
    overflow:hidden;
}

.container {
    text-align:center;
    background:white;
    padding:25px;
    border-radius:15px;
    box-shadow:0 10px 20px rgba(0,0,0,0.15);
    max-width:400px;
    width:90%;
    z-index:2;
}

h1 { color:#E55451; }
.heart { font-size:50px; color:#E55451; }

button {
    padding:12px 20px;
    margin:8px;
    border:none;
    border-radius:8px;
    cursor:pointer;
    font-size:16px;
    transition:0.3s;
}

#yesButton { background:#4CAF50; color:white; }
#noButton { background:#f44336; color:white; position:relative; }

#responseMessage { margin-top:10px; color:#E55451; display:none; }

/* popup */
.popup {
    position:fixed;
    top:0; left:0;
    width:100%; height:100%;
    display:none;
    justify-content:center;
    align-items:center;
    background:rgba(0,0,0,0.6);
    z-index:3;
}

.popup-content {
    background:white;
    padding:25px;
    border-radius:15px;
    text-align:center;
    animation:pop 0.4s ease;
}

@keyframes pop {
    from{transform:scale(0.5);}
    to{transform:scale(1);}
}

/* hearts animation */
.heart-float {
    position:fixed;
    bottom:-20px;
    font-size:20px;
    animation:floatUp 4s linear infinite;
    color:#ff4d6d;
}

@keyframes floatUp {
    from {transform:translateY(0);}
    to {transform:translateY(-110vh);}
}
</style>
</head>

<body>

<div class="container">
    <h1>Will you be my Valentine? 💖</h1>
  

    <button id="yesButton">Yes 💖</button>
    <button id="noButton">No 😢</button>

    <p id="responseMessage"></p>
</div>

<!-- popup -->
<div class="popup" id="lovePopup">
    <div class="popup-content">
        <h2>💖 My Love 💖</h2>
        <p>
        Alfiyaaa Shoaib Khan... You are my valentine 💋 baby..<br><br>
        Mai khush huu betee tumkoo apnaa har chiz bana kr mai tumkoo hameshaa khush rakhungaa 💖<br><br>
        Dil aur jaan aur har chiz tumpai kurban krungaa..<br>
        Hamesha tumrha sath dunga tumko pyaar krunga tumrhe sath marunga aur jiyugaa...<br><br>
        Tumrhe sath sapne dekhungaa 🫣❤️<br><br>
        Betee I Love You.... You are My Valentine 😍🫠
        </p>

        <button onclick="closePopup()">Close 💞</button>
    </div>
</div>

<script>
const yesBtn=document.getElementById("yesButton");
const noBtn=document.getElementById("noButton");
const msg=document.getElementById("responseMessage");
const popup=document.getElementById("lovePopup");

let yesScale=1;
let noScale=1;

// YES CLICK
yesBtn.onclick=()=>{
    popup.style.display="flex";
    startHearts();
};

// NO CLICK
noBtn.onclick=()=>{
    yesScale+=0.3;
    yesBtn.style.transform=`scale(${yesScale})`;

    noScale-=0.2;
    if(noScale<0.3) noScale=0.3;
    noBtn.style.transform=`scale(${noScale})`;

    yesBtn.innerText="Say YES ❤️ Please";
    msg.innerText="Soch lo jaan 😢";
    msg.style.display="block";
};

// RUN AWAY BUTTON
noBtn.addEventListener("mouseover",moveBtn);
noBtn.addEventListener("touchstart",moveBtn);

function moveBtn(){
    let x=Math.random()*200-100;
    let y=Math.random()*200-100;
    noBtn.style.transform=`translate(${x}px,${y}px) scale(${noScale})`;
}

// CLOSE POPUP
function closePopup(){
    popup.style.display="none";
}

// HEARTS EFFECT
function startHearts(){
    for(let i=0;i<40;i++){
        let heart=document.createElement("div");
        heart.innerHTML="💞";
        heart.className="heart-float";
        heart.style.left=Math.random()*100+"%";
        heart.style.fontSize=(15+Math.random()*25)+"px";
        heart.style.animationDuration=(2+Math.random()*3)+"s";
        document.body.appendChild(heart);

        setTimeout(()=>heart.remove(),4000);
    }
}
</script>

</body>
</html>
